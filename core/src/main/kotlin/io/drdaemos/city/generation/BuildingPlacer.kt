package io.drdaemos.city.generation

import io.drdaemos.city.data.terrain.TerrainRegion
import io.drdaemos.city.spatial.BoundingBox
import io.drdaemos.city.spatial.Position

class BuildingPlacer (seed: Long = 0L) {
    fun randomBuildingPosition(regions: List<TerrainRegion>) {

    }

    private fun isPositionAvailable(position: Position): Boolean {
        return false
    }
}